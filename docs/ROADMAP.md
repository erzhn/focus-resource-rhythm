# Дорожная карта до «полноценного продукта»

Прогресс: `[x]` готово и проверено · `[~]` код готов, ждёт ключей/БД для проверки · `[ ]` впереди.

## Приоритет 1 — реальная персистентность
- [x] **P1.1** DataProvider: абстракция (`src/lib/data/*`) + Demo/Supabase реализации; стор гидратирует снимок и персистит мутации через провайдер, выбор по `isSupabaseConfigured`. Демо проверено в браузере; Supabase-путь ждёт БД (P1.2).
- [ ] **P1.2** Проверка на живой БД (миграции, RLS, вход). *Требует Supabase-проект + `.env.local`.*
- [x] **P1.3** Мастер онбординга (`/onboarding`, 3 шага) + запись настроек, redirect не-онбординутых в proxy, кнопка «Выйти» в оболочке (реальный режим). Демо-поток проверен в браузере.

## Приоритет 2 — пользовательские сценарии
- [x] **P2.1** Модал полного редактирования задачи (`task-edit.tsx`): все поля, статус, результат/критерий/действие, редактор повторений (все типы) и зависимостей с защитой от циклов (`domain/tasks/dependencies` + тесты). Кнопка «изменить» в строке задачи. Демо проверено в браузере.
- [x] **P2.2** Личные события (CRUD через модал в календаре: фикс/гибкое, blocks_availability) + временные блоки задач (время в редакторе, показ в дне, конфликты событий+блоков). Демо проверено.

## Приоритет 3 — синхронизация календарей до конца
- [ ] **P3.1** Sync-движок + выбор календарей + экран «было/стало». *Проверка ждёт OAuth-ключей.*
- [ ] **P3.2** Webhooks + отправка задач наружу + продление подписок. *Проверка ждёт ключей.*

## Приоритет 4 — качество и полнота
- [ ] **P4.1** Статистика с графиками (Recharts).
- [ ] **P4.2** E2E (Playwright) + интеграционные тесты.

Готовые промпты для каждой задачи — см. историю обсуждения; выполняются по порядку.
