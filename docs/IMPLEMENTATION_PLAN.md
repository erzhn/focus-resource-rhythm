# План реализации — «Фокус — Ресурс — Ритм»

Статус на 2026-08-05. Отмечено честно: `[x]` — сделано и проверено, `[~]` — частично/каркас, `[ ]` — впереди.

## Этап 0. Аудит и проектирование
- [x] Аудит окружения: целевой каталог `D:\PET-projects\planning` (пустой), стек Next.js.
- [x] Ключевое ограничение: нет Docker и ключей Supabase → выбран режим «реальный код + честный демо-режим».
- [x] Зафиксированы решения в [DECISIONS.md](DECISIONS.md).

## Этап 1. Основа приложения
- [x] Next.js 16 (App Router) + TypeScript strict + Tailwind v4.
- [x] Централизованное имя продукта и региональные настройки (`src/config/app.ts`).
- [x] Адаптивный layout: боковая навигация (desktop) + нижняя (mobile) + плавающая кнопка добавления.
- [x] Полная схема PostgreSQL (26 таблиц) + RLS + триггеры (`supabase/migrations/*`).
- [~] Supabase Auth — схема (профиль/настройки автосоздаются триггером `handle_new_user`); UI входа появится при подключении ключей.

## Этап 2. Планы, цели и задачи
- [x] Сферы, цели, проекты, задачи (демо-данные + схема).
- [x] Зоны фокуса «Сейчас / Следом / Позже / Отказ».
- [x] Лимит трёх активных результатов с вариантами (завершить/приостановить/перенести/отменить).
- [x] Быстрый ввод задачи + обязательный вопрос о формулировке + шаблонный помощник.
- [~] Зависимости и ближайшие действия — в доменной логике и схеме; UI-редактор зависимостей впереди.

## Этап 3. Приоритеты и ресурсы
- [x] Объяснимый алгоритм приоритета 0–100 с разбором факторов (`src/domain/priority`).
- [x] Ручной приоритет отдельно от системного + сравнение.
- [x] План/факт и лимиты времени, сил и денег; предупреждения (`src/domain/resources`).

## Этап 4. Ежедневный и еженедельный ритм
- [x] Утренняя проверка (силы, время) и построение черновика плана дня (1 главное + до 2 доп + повторяющиеся).
- [x] Подтверждение плана дня.
- [x] Повторения: все типы + исключения экземпляров (`src/domain/recurrence`, покрыто тестами).
- [~] Вечерний итог и еженедельная сверка — схема готова, мастера UI впереди (см. /reviews).
- [x] Внутренние уведомления (выводятся из состояния, ведут к действию).

## Этап 5. Календарь
- [~] Схема событий (фиксированные/гибкие, blocks_availability) и детектор конфликтов (`src/domain/schedule`).
- [ ] Режимы день/неделя/месяц/год и drag-and-drop — впереди (см. /calendar).

## Этап 6. Google и Outlook
- [~] Полная схема синхронизации (подключения, выбор календарей, связи, sync-состояния, запросы подтверждения, версии).
- [ ] OAuth-потоки, адаптеры провайдеров, webhook/delta, экран конфликтов — код-каркас по схеме; активация ожидает ключей.

## Этап 7. Дашборд, качество и сдача
- [x] Дашборд «Сегодня» на реальных вычислениях.
- [x] Пустые/предупреждающие состояния; честные плейсхолдеры без кнопок-обманок.
- [x] Адаптивность (375/768/1440), доступность (роли, aria, focus-visible, контраст, дубли цвета текстом).
- [x] Unit-тесты (44) зелёные; lint, typecheck, production build проходят.
- [~] Интеграционные и E2E тесты — требуют поднятой БД (нет Docker в среде); сценарии описаны в [TESTING.md](TESTING.md).

## Что дальше (приоритет)
1. Подключить реальный Supabase-провайдер данных за существующим интерфейсом стора.
2. Мастера вечернего итога и еженедельной сверки.
3. Календарные режимы + drag-and-drop (dnd-kit).
4. OAuth Google/Microsoft + двусторонний sync + экран конфликтов.
