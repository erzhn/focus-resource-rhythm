/**
 * Централизованная конфигурация продукта.
 * Рабочее название хранится здесь, чтобы его можно было заменить в одном месте.
 */
export const APP = {
  /** Рабочее название продукта. */
  name: "Фокус — Ресурс — Ритм",
  shortName: "ФРР",
  tagline: "Личная система планирования: важное не вытесняется срочным",
} as const;

/** Начальные региональные настройки (значения по умолчанию для новых пользователей). */
export const REGIONAL_DEFAULTS = {
  /** Часовой пояс пользователя по умолчанию (IANA). Метки времени в БД хранятся в UTC. */
  timezone: "Asia/Bishkek",
  /** 24-часовой формат времени. */
  hour12: false,
  /** Формат даты в интерфейсе. */
  dateFormat: "dd.MM.yyyy",
  /** Валюта по умолчанию (ISO 4217). */
  currency: "KGS",
  /** Начало недели: 1 = понедельник (соответствует ISO). */
  weekStartsOn: 1 as const,
} as const;

/**
 * Правила методики «Фокус — Ресурс — Ритм», вынесенные в конфигурацию,
 * чтобы доменные модули и тесты ссылались на единый источник истины.
 */
export const METHOD = {
  /** Не больше трёх активных крупных результатов одновременно (зона «Сейчас»). */
  maxActiveResults: 3,
  /** Одно главное дело в день. */
  mainTasksPerDay: 1,
  /** До двух дополнительных задач в день (не считая обязательных повторяющихся). */
  maxSecondaryTasksPerDay: 2,
  /** Ключевых результатов недели — до трёх. */
  maxWeeklyResults: 3,
  /** Резерв времени по умолчанию (доля свободного времени). */
  defaultTimeReserveRatio: 0.25,
  /** Допустимый диапазон резерва времени. */
  minTimeReserveRatio: 0.2,
  maxTimeReserveRatio: 0.3,
} as const;

export type Currency = string;
