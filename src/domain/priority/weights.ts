/**
 * Веса и правила расчёта приоритета вынесены в отдельный модуль,
 * чтобы их можно было настраивать и покрывать тестами независимо от UI.
 * Базовая конфигурация из ТЗ §6.
 */
export const PRIORITY_WEIGHTS = {
  /** Важность: до 25 баллов. */
  importance: 25,
  /** Срочность и близость срока: до 20 баллов. */
  urgency: 20,
  /** Последствия невыполнения: до 15 баллов. */
  consequence: 15,
  /** Связь с активной целью/результатом 90 дней: до 15 баллов. */
  goalLink: 15,
  /** Разблокирование других задач и зависимости: до 10 баллов. */
  unblock: 10,
  /** Соответствие доступным времени, силам и деньгам: до 10 баллов. */
  resourceFit: 10,
  /** Просрочка: до 5 дополнительных баллов (в пределах общего максимума 100). */
  overdue: 5,
} as const;

/** Общий максимум балла. */
export const PRIORITY_MAX = 100;

/** Горизонт (в днях), на котором срочность линейно растёт до максимума. */
export const URGENCY_HORIZON_DAYS = 14;

/** Сколько баллов даёт разблокирование одной зависимой задачи. */
export const UNBLOCK_POINTS_PER_TASK = 5;

/** Разбивка «соответствия ресурсам» по компонентам (в сумме = weights.resourceFit). */
export const RESOURCE_FIT_SPLIT = {
  time: 4,
  energy: 3,
  money: 3,
} as const;
